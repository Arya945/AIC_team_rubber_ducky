# .github/workflows/cd.yml
# Alpha 0.1.1 Ai Edition
# W.I.P With hate Arya

name: AIC Robotic CD Pipeline

on:
  push:
    branches: ["main"]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push-image:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.TK }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest
            type=sha,format=short

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./docker
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: Test Docker image
        run: |
          docker run --rm ghcr.io/${{ github.repository }}:latest python -c "print('congrats is running successfully!')"

      - name: Deployment Summary
        run: |
          echo "CD Pipeline Complete!"
          echo "Image uploaded to: ghcr.io/${{ github.repository }}"
          echo "Tags: latest, sha-${GITHUB_SHA::7}"
